# Long-term caching for content-hashed build artifacts (Workers static assets).
# index.html keeps the platform default (revalidate) so new deploys are picked up.
/static/*
  Cache-Control: public, max-age=31536000, immutable
/assets/*
  Cache-Control: public, max-age=31536000, immutable
